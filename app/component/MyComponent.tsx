import React from 'react';

// 定義並導出一個名為 MyComponent 的 React 函數組件
const MyComponent: React.FC = () => {
  return <div>Hello, World!</div>; // 返回一個包含文本 "Hello, World!" 的 div 元素
};

export default MyComponent; // 將組件導出，以便在其他文件中使用
